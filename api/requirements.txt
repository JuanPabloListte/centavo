# Versiones con las que pasa la suite completa, en CPython 3.14.
# Sólo lo que el código importa (más uvicorn, que se ejecuta).

# API
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4

# Parseo
pdfplumber==0.11.10

# Persistencia
psycopg[binary]==3.3.5
python-dotenv==1.2.3

# Modelo local
httpx==0.28.1

# Tests
pytest==9.0.2
